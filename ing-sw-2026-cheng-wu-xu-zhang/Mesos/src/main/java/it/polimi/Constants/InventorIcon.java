package it.polimi.Constants;

/**
 * Represents the various icons found on Inventor cards.
 * These icons are used for scoring and specialized building effects.
 */
public enum InventorIcon {
    /** Bread icon. */
    bread,
    /** Rope icon. */
    rope,
    /** Flute icon. */
    flute,
    /** Bowl icon. */
    bowl,
    /** Hide icon. */
    hide,
    /** Hook icon. */
    hook,
    /** Doll icon. */
    doll,
    /** Boat icon. */
    boat,
    /** Arrow icon. */
    arrow,
    /** Necklace icon. */
    necklace
}
